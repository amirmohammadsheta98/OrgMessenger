@inherits LayoutComponentBase

@using System.Security.Claims
@using OrgMessenger.Components.Pages


<RadzenComponents @rendermode="InteractiveServer" />
@* @inject AuthenticationStateProvider AuthProvider *@
@inject NavigationManager Navigation

<RadzenLayout>
    <RadzenHeader Style="padding: 0 16px;">
        <div style="display: flex; justify-content: space-between; align-items: center; width: 100%;">

            <!-- راست: لوگو و نام شرکت -->
            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0">
                <RadzenSidebarToggle Click="@(() => sidebar1Expanded = !sidebar1Expanded)" />
                <div style="display: flex; align-items: center;">
                    <img src="/Images/pricrIcon.png" alt="لوگو" style="height: 32px; margin-left: 10px;" />
                    <RadzenLabel Text="پیام رسان سازمانی تل چت" />
                </div>
            </RadzenStack>
            <Header />

        </div>
    </RadzenHeader>

    <RadzenSidebar @bind-Expanded="@sidebar1Expanded">
        <RadzenPanelMenu>
            <RadzenPanelMenuItem Path="/" Text="داشبورد" Icon="home" />
            <GetUsers />
        </RadzenPanelMenu>

    </RadzenSidebar>
    <RadzenBody>
        <RadzenAppearanceToggle class="rz-mx-2" />

        <div class="rz-p-4">
            @Body
        </div>
    </RadzenBody>
    @*  <RadzenFooter>

    </RadzenFooter> *@
</RadzenLayout>

@code {
    bool sidebar1Expanded = true;


    // private UserDto user;

    // protected override async Task OnInitializedAsync()
    // {
    //     var authState = await AuthProvider.GetAuthenticationStateAsync();
    //     if (!authState.User.Identity.IsAuthenticated)
    //     {
    //         Navigation.NavigateTo("/auth");
    //     }

    //     user = GetClaim.Get(authState.User);
    // }
    // public string GetRole(string role)
    // {
    //     var res = "";
    //     switch (role)
    //     {
    //         case "SupperAdmin": res = "مدیر کل سیستم"; break;
    //         case "AdminFinanicial": res = "مدیر مالی"; break;
    //         case "Finanicial": res = "کارشناس وصول مطالبات"; break;
    //         case "AdminSale": res = "مدیر فروش"; break;
    //         case "Sale": res = "کارشناس فروش"; break;
    //         case "AdminShopping": res = "مدیر بازرگانی"; break;
    //         case "Shopping": res = "کارشناس خرید"; break;
    //         default: res = "نامشخص"; break;
    //     }
    //     return res;
    // }
}